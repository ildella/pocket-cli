{
  "name": "pocket-server",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.16.4",
    "tracer": "^0.9.6",
    "webtask-tools": "^3.4.0"
  }
}
