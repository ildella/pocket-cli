{
  "name": "pocket-server",
  "version": "0.1.0",
  "engines": {
    "node": "8"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "tracer": "^0.9.6",
    "webtask-tools": "^3.4.0"
  }
}
