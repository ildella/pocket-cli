{
  "name": "pocket-proxy-server",
  "version": "0.1.0",
  "engines": {
    "node": "8"
  },
  "scripts": {
    "start": "wt serve pocket-proxy-server.js",
    "create": "wt create pocket-proxy-server.js -b --secrets-file .authproxy.env",
    "update": "wt update pocket-proxy-server pocket-proxy-server.js -b",
    "dev": "wt create pocket-proxy-server.js -b --secrets-file .authproxy.env --name pocket-proxy-server-dev",
    "update:dev": "wt update pocket-proxy-server-dev pocket-proxy-server.js -b -w"
  },
  "dependencies": {
    "axios": "0.19.0",
    "express": "4.17.1",
    "tracer": "1.0.1",
    "webtask-tools": "3.4.1"
  }
}
